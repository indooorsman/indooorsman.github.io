#!/usr/bin/env bash
cd ..
harp compile . ./www/
rm -rf ./www/.idea ./www/bin
cp -a ./www/* ../
rm -rf ./www/*
cd ..
git status
read -p 'sure to commit and push? (y/n) ' SURE
if [ "$SURE"="y" ]; then
  git add .
  git commit -m "新的文章"
  git push
fi